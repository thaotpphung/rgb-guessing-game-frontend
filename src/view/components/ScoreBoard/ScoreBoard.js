import React from 'react';
import { useDispatch, useSelector } from 'react-redux';
import Modal from '../../common/Modal/Modal';

const ScoreBoard = () => {
  return <Modal label="High Scores"></Modal>;
};

export default ScoreBoard;
